<style>
  .change {
    float: right;
    display: block;
    background: #547498;
  }


</style>

<h3> Dominion Strategy Forum </h3>
<ul>
  <% boards.each(function(board) { %>
  <li class="topic-container">
    <div class="topic">
      <a href="#/boards/<%= board.id %>">
        <%= board.escape("title") %>
      </a>
    </div>

    <% if (DominionStrategyClone.isAdmin) { %>
      <div class="change">
          <button id='<%= board.id %> '>
             REMOVE BOARD
          </button>
      </div>
      <div class="change">
        <a href="#/boards/<%= board.id %>/edit">
          EDIT BOARD
        </a>
      </div>
    <% } %>
  </li>
  <% }) %>
</ul>
